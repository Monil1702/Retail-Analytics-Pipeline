[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "retail-analytics-pipeline"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["pyspark==3.5.3", "psycopg[binary]==3.2.3"]

[tool.setuptools.packages.find]
where = ["src"]
